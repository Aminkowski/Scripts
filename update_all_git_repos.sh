#! /bin/bash
per
git fetch
git diff --stat
git add .
git commit -m "routine sync via script"
git push
back
dot
git fetch
git diff --stat
git add .
git commit -m "routine sync via script"
git push
back
code && cd Scripts
git fetch
git diff --stat
git add .
git commit -m "routine sync via script"
git push
back
